export enum PriceValue {
  EXACT = 'exact',
  RANGE = 'range',
  FROM = 'from'
};

export enum PriceContext {
  RETAIL = 'retail',
  WHOLESALE = 'wholesale',
  ESTIMATE = 'estimate',
  HISTORICAL = 'historical'
};

export enum PriceTax {
  GROSS = 'gross',
  NET = 'net',
  UNKNOWN = 'unknown'
};
